name: sql_env
version: "1.0.0"
description: "SQL Query Learning Environment - an OpenEnv-compatible RL environment where AI agents learn to write correct and efficient SQL queries against a realistic e-commerce database with 3 difficulty tiers (easy/medium/hard), 9 tasks, automated graders, and meaningful reward functions (0.0-1.0)."

action: SQLAction
observation: SQLObservation
state: SQLState
